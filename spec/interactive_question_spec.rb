require 'spec_helper'

describe InteractiveQuestion do
  it 'has a version number' do
    expect(InteractiveQuestion::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
